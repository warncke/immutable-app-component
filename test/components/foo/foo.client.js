'use strict'

module.exports = {
    binds: [
        'checkboxInput',
        'radioInputNo',
        'radioInputMaybe',
        'radioInputYes',
        'select',
        'textarea',
        'textInput',
    ],
    refreshInterval: 1000,
    preRefresh: function (args) {

    },
    preRender: function (args) {

    },
}